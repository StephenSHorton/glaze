{
  "name": "glaze-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Glaze — shaders as a reactive, accessible-by-default paint primitive for the web.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @glaze/core && npm run build -w @glaze/svelte",
    "typecheck": "npm run typecheck -w @glaze/core && npm run typecheck -w @glaze/svelte",
    "dev:demo": "npm run dev -w glaze-svelte-demo",
    "build:demo": "npm run build -w glaze-svelte-demo"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18"
  }
}
